import * as React from "react";
import { StyleSheet, View } from "react-native";

const GooglePlay = () => {
  return <View style={styles.googlePlay} />;
};

const styles = StyleSheet.create({
  googlePlay: {
    width: 24,
    height: 24,
    overflow: "hidden",
  },
});

export default GooglePlay;
